import React from 'react';
import NetworkingManager from '../components/NetworkingManager';

const NetworkingManagerPage: React.FC = () => {
  return <NetworkingManager />;
};

export default NetworkingManagerPage;
