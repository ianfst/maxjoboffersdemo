import { searchJobs as originalSearchJobs } from '../../../actions/job';

// Re-export the original searchJobs function
export const searchJobs = originalSearchJobs;

// Export other job-related actions as needed
